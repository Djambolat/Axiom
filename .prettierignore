# Здесь текст свёрстан не тегами, а переносами строк прямо в исходнике:
# у .content__text стоит white-space: pre-line, поэтому каждый перенос
# и каждая пустая строка — это часть вёрстки, а не форматирование.
#
# Prettier об этом не знает и переносит строки по ширине 80 символов —
# абзацы слипаются, а текст рвётся посреди предложения.
#
# Страницы, где текст размечен нормальными <p> внутри .content--article
# (hw-7, bem), форматтеру не мешают и здесь не перечислены.

homeworks/hw-9.html
homeworks/hw-template.html
pages/categories/git.html
